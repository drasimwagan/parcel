"""Claude-backed module generator.

Public entry points:

- :func:`parcel_shell.ai.generator.generate_module` — the pipeline.
- :mod:`parcel_shell.ai.provider` — the :class:`ClaudeProvider` Protocol and
  both implementations (API + CLI).
"""
