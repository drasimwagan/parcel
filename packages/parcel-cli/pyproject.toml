[project]
name = "parcel-cli"
version = "0.0.0"
description = "Parcel CLI — new-module scaffolder, dev server launcher, module installer"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    # Phase 6 will populate (typer, rich, jinja2, etc.).
]

[project.scripts]
# Phase 6 will wire this up:
# parcel = "parcel_cli.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parcel_cli"]
